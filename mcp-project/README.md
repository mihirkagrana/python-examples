### Create Local MCP Server 

Setup and Installation 

```
  - python -m venv .mcp-venv 
  - pip install fastmcp 
  - check with MCP Inspector: fastmcp dev server.py
  - start the MCP server: fastmcp run server.py

```